# managablereverseproxy
Reverse proxy managed by a web GUI


```py
managablereverseproxy/
    proxycomponents/
        firewall/
            frontend/
                template.html
                main.css
                main.js
            
            firewall.py
            """
            Does firewall things, listens to some endpoints config requests with websocket, sends traffic data via websocket,
            A parent class would
            combine all template.html files in a list???
            """
        components/
```